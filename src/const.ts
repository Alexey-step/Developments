export enum APIRoutes {
  CARDS = "/homes",
}

export enum AppRoutes {
  MAIN = "/",
  NOT_FOUND = "/not-found",
  DETAILS = "/details",
}

export enum Status {
  PENDING = "pending",
  LOAD = "load",
  LOADED = "loaded",
  ERROR = "error",
}

export const ERROR_TIMEOUT = 3000;
